---
id: 005
status: DONE
reviewer: sonnet
milestone: v1
---
# Task 005: Phase 1 port — vector geometry summary

## Objective

Port summarize_vector_geometry() from fitting_functions.R to dkg.phases.phase1. Add LOO robustness checks for the three moment-based shape statistics not present in the R version.

## Acceptance Criteria

- [x] Output dict contains all fields produced by the R function with matching names
- [x] LOO fields present: skewness_loo_max_influence, kurtosis_loo_max_influence, bimodality_loo_max_influence, skewness_is_robust, kurtosis_is_robust, bimodality_is_robust
- [x] `sweep_phase1(matrix, col_names, config)` returns a Polars DataFrame with one row per column
- [x] `filter_by_geometry(df, config)` returns the DataFrame with geometry_keep and geometry_filter_reason columns added
- [x] Constant or all-NaN vectors return a sentinel row with geometry_status != "ok" rather than raising
- [x] All default checks pass

## See Also

- fitting_functions.R

## Outputs

- src/dkg/phases/phase1.py
- tests/test_phase1.py

## Requirements

- Port all fields from summarize_vector_geometry(): completeness counts, quantiles (0/1/5/10/25/50/75/90/95/99/100), mean, sd, mad, IQR, zero_frac, near_zero_var, bin_n_min/max/imbalance (n_bins=5), skewness, excess kurtosis, bimodality coefficient (skew²+1)/kurtosis, Hartigan dip statistic and p-value (use diptest package or implement directly), density peak/valley count and ruggedness from scipy KDE, effective support size and fraction, left/right tail span, tail asymmetry ratio
- LOO robustness: for each of skewness, excess kurtosis, and bimodality coefficient, draw min(n, config.n_loo) subsamples of size n-1 without replacement using numpy.random.default_rng(config.seed); compute max absolute change across subsamples; set {stat}_is_robust = True if max_influence < config.loo_influence_threshold * abs(full_stat)
- `sweep_phase1(matrix: np.ndarray, col_names: list[str], config: RunConfig) -> pl.DataFrame` applies summarize_phase1 to each column
- `filter_by_geometry` mirrors R's filter_features_by_geometry() with same default thresholds
- Add diptest to project dependencies via uv add

## Blocked By

- 002

## Reviewer Notes

VERDICT: PASS

CRITERIA:
- **Output dict contains all fields from R function with matching names**: `_REQUIRED_FIELDS` list in tests enumerates all required fields, and `test_all_required_fields_present` verifies each is present in the output. The `summarize_phase1` return dict and `_sentinel` dict both contain all 48 fields.
- **LOO fields present**: All six LOO fields (`skewness_loo_max_influence`, `kurtosis_loo_max_influence`, `bimodality_loo_max_influence`, `skewness_is_robust`, `kurtosis_is_robust`, `bimodality_is_robust`) are present in both the return dict and `_sentinel`, verified by `test_loo_fields_present_and_typed`.
- **`sweep_phase1` returns Polars DataFrame with one row per column**: Implemented at phase1.py:246–257; `test_sweep_phase1_shape` confirms shape `(10, ...)` for a 220×10 matrix.
- **`filter_by_geometry` adds `geometry_keep` and `geometry_filter_reason` columns**: Implemented at phase1.py:261–310; confirmed by `test_filter_by_geometry_adds_columns`.
- **Constant/all-NaN vectors return sentinel with `geometry_status != "ok"`**: `_sentinel()` returns `geometry_status="no_complete_values"` or `"constant_or_near_constant"`; confirmed by `test_constant_vector_sentinel`, `test_all_nan_vector_sentinel`, `test_sentinel_does_not_raise_for_single_value`.
- **All default checks pass**: Audit shows `54 passed, 2 warnings in 3.35s` with exit code 0, plus ruff, ruff format, and mypy all pass.

SUMMARY:
All six acceptance criteria are satisfied with concrete evidence from the implementation and test suite. The audit confirms a clean run with 54 passing tests, no lint or type errors, and correct sentinel behavior for degenerate inputs. Task 005 is ready to merge.
