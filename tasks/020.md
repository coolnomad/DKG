---
id: 020
status: DONE
reviewer: sonnet
milestone: v1
---
# Task 020: CLI entry point

## Objective

Implement dkg.cli and dkg.__main__ using argparse so the package is fully runnable from the command line and dispatches to the correct run mode.

## Acceptance Criteria

- [ ] `python -m dkg --help` exits 0 and lists all flags
- [ ] `python -m dkg --version` exits 0 and prints the package version
- [ ] `python -m dkg --mode xy --x-matrix X.csv --y-matrix Y.csv --output-dir out` dispatches to modes.xy.run()
- [ ] `--config-json path` loads a JSON file and merges into RunConfig before CLI flags are applied
- [ ] Invalid mode exits 1 with a clear error message to stderr
- [ ] All default checks pass

## Inputs

- src/dkg/config.py
- src/dkg/modes/xy.py
- src/dkg/modes/xx.py
- src/dkg/modes/pair.py
- src/dkg/__main__.py

## Outputs

- src/dkg/cli.py
- src/dkg/__main__.py
- tests/test_cli.py

## Requirements

- Top-level flags: --mode {xy,xx,pair}, --x-matrix, --y-matrix (optional for xx mode), --output-dir, --config-json, --n-jobs, --tier1-threshold, --top-k, --graph-edge-threshold, --pair-x, --pair-y
- Parse flags into RunConfig; --config-json loads JSON and sets RunConfig fields before CLI flag overrides are applied
- Dispatch to modes.xy.run(), modes.xx.run(), or modes.pair.run() based on mode
- Print one-line completion summary and output directory path on success
- --version reads from importlib.metadata.version("dkg")
- Exit 0 on success, 1 on any error with message to stderr

## Blocked By

- 017
- 018
- 019

## Reviewer Notes

VERDICT: FAIL
CRITERIA:
- [PASS] `--help` exits 0: `test_help_exits_zero` asserts `exc_info.value.code == 0`; argparse `--help` always exits 0.
- [PASS] `--version` exits 0 and prints version: `test_version_exits_zero` checks `exc_info.value.code == 0` and `"dkg" in captured.out`; `_get_version()` uses `importlib.metadata.version("dkg")`.
- [PASS] Dispatches to `modes.xy.run()`: `test_dispatch_xy` patches and asserts `mock_run.assert_called_once()` with correct config fields.
- [PASS] `--config-json` merges before CLI flags: `test_config_json_cli_overrides` confirms JSON base `top_k=500` is overridden to `999` by `--top-k 999`.
- [FAIL] Invalid mode exits **1** with error to stderr: argparse `choices=["xy","xx","pair"]` validation calls `parser.error()` which exits with code **2**, not 1. No custom error handler overrides this exit code in `cli.py`, and no test asserts this behavior. The acceptance criterion requires exit code 1 but the implementation produces exit code 2.
- [PASS] All default checks pass: audit shows ruff, mypy, and pytest (328 passed, 1 skipped) all exit 0.

SUMMARY:
Five of the six acceptance criteria are met with concrete test coverage and clean static analysis. The single failing criterion is the invalid-mode exit code: argparse's built-in `choices` validation exits with code 2, not 1 as required, and no subclass or `try/except SystemExit` wrapper intercepts and remaps this. The fix is either to call `parser.parse_known_args` + manual validation that calls `sys.exit(1)`, or to override `parser.error` to exit with 1 instead of 2. A corresponding test asserting `rc == 1` on an invalid mode is also missing.

VERDICT: PASS
CRITERIA:
- [PASS] `--help` exits 0: `test_help_exits_zero` asserts `exc_info.value.code == 0`; argparse `--help` always exits 0.
- [PASS] `--version` exits 0 and prints version: `test_version_exits_zero` checks `exc_info.value.code == 0` and `"dkg" in captured.out`; `_get_version()` uses `importlib.metadata.version("dkg")`.
- [PASS] Dispatches to `modes.xy.run()`: `test_dispatch_xy` patches and asserts `mock_run.assert_called_once()` with `config.mode == "xy"`, correct paths, and output dir.
- [PASS] `--config-json` merges before CLI flags: `test_config_json_cli_overrides` confirms JSON base `top_k=500` is overridden to `999` by `--top-k 999`.
- [PASS] Invalid mode exits **1** with error to stderr: `_Parser` subclass overrides `error()` to call `self.exit(1, ...)`, so argparse `choices` validation exits with code 1 (not 2). `test_invalid_mode_exits_one` asserts `exc_info.value.code == 1` and `"error" in capsys.readouterr().err.lower()`. This directly addresses the previous reviewer's FAIL criterion.
- [PASS] All default checks pass: audit shows ruff, mypy, and pytest (329 passed, 1 skipped) all exit 0.

SUMMARY:
The previous FAIL has been resolved. The `_Parser` subclass overrides `argparse.ArgumentParser.error()` to call `self.exit(1, ...)` instead of the default exit code 2, so invalid `--mode` values now exit with code 1. A corresponding test `test_invalid_mode_exits_one` verifies both the exit code and the presence of "error" in stderr. All six acceptance criteria are now met with concrete test coverage and clean static analysis.
