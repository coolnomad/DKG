VERDICT: PASS
CRITERIA:
- **`run_deep(tier1_df, X, Y, x_cols, y_cols, phase1_x, phase1_y, config)` returns a Polars DataFrame** — Function signature at `tier2.py:83` matches exactly; return type is `pl.DataFrame`; verified by `test_returns_dataframe`.
- **Output has x_col and y_col prepended; all phase 2-9 output columns follow** — `tier2.py:118-120` explicitly reorders with `result.select(["x_col", "y_col", *other_cols])`; verified by `test_x_col_y_col_first` and `test_phase_columns_present`.
- **Pairs where a phase raises are recorded with NaN for that phase's columns; run continues** — Each phase is wrapped in independent `try/except` blocks (`tier2.py:38-75`); on exception only that phase's keys are absent (filled as null via `rec.get(k)` normalisation at line 112); `test_phase_failure_does_not_abort` confirms all pairs survive and other phases still produce columns.
- **tier2_deep.parquet written to config.output_dir** — `out_path = Path(config.output_dir) / "tier2_deep.parquet"` at line 97; written in both empty and non-empty branches; verified by `test_parquet_written`, `test_parquet_written_when_no_pairs`, and `test_parquet_readable_and_matches`.
- **joblib parallelism with config.n_jobs** — `Parallel(n_jobs=config.n_jobs, backend="loky")` at `tier2.py:113`.
- **All default checks pass** — Audit shows ruff, mypy, and pytest all exit 0; 285 tests passed.

SUMMARY:
All acceptance criteria are met with direct evidence from the implementation. The function signature, column ordering, per-phase exception handling with continuation, parquet output, and joblib parallelism are all correctly implemented and covered by a comprehensive test suite that passed cleanly in CI.
