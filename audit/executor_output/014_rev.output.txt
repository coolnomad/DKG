VERDICT: PASS
CRITERIA:
- **Output columns match spec**: All 16 required columns present (`predictor, target, n, source, metric, mean, sd, q025, median, q975, ci_width, relative_cv, sign_consistency, sign_positive_frac, sign_negative_frac, n_success`). Verified in `_EXPECTED_COLUMNS` list and `test_all_columns_present`. The extra `stability_status` column is additive and not a violation.
- **Both bootstrap and subsample sources present**: `_build_rows` called for both `"bootstrap"` and `"subsample"` in `summarize_phase10`. `test_sources_present` asserts `sources == {"bootstrap", "subsample"}`.
- **All 12 tracked metrics present**: `_TRACKED_METRICS` list contains exactly the 12 required metrics. `test_all_metrics_present` and `test_12_metrics_per_source` verify all 12 appear for each source. `test_row_count` confirms 24 total rows.
- **Derived columns correct**: `ci_width = q975 - q025` (phase10.py line `ci_width = s["q975"] - s["q025"]`); `relative_cv = sd / (abs(mean) + 1e-8)` (matching spec exactly); `sign_consistency = max(sign_positive_frac, sign_negative_frac)`. Tests `test_ci_width_derived_correctly`, `test_relative_cv_derived_correctly`, `test_sign_consistency_derived_correctly` verify all three.
- **All default checks pass**: Audit shows `273 passed, 3 warnings in 21.03s` with exit code 0. Ruff, mypy, and pytest all green.
- **Two resampling schemes with correct sizes**: Bootstrap uses `rng.integers(0, n, size=n)` (with replacement, size=n); subsample uses `rng.choice(n, size=sub_n, replace=False)` where `sub_n = floor(n * sample_frac)`.
- **Phases 2, 5, 7, 8 called per resample**: `_run_one` calls `summarize_phase2`, `summarize_phase5`, `summarize_phase7`, `summarize_phase8`.
- **Exception handling and n_success**: `_run_one` catches `Exception` and returns `None`; `_summarize_metric` counts `len(finite)` as `n_success`.
- **Sentinel for insufficient data**: Returns single-row sentinel when `n < 30`, `unique(x) < 10`, or `std(y) == 0`. Tests `test_insufficient_data_small_n`, `test_insufficient_data_constant_target`, `test_insufficient_data_few_unique_x` all verify.
- **Seeded RNG**: `np.random.default_rng(seed)` used; `test_seed_reproducibility` confirms determinism.

SUMMARY:
All acceptance criteria are satisfied with concrete evidence from the implementation and test suite. The 24-row output structure (12 metrics × 2 sources), all required columns, derived field formulas, sentinel logic, and seeded reproducibility are correctly implemented and tested. The audit shows a clean pass across ruff, mypy, and 273 pytest tests.
