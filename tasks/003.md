---
id: 003
status: DONE
reviewer: sonnet
milestone: v1
---
# Task 003: Matrix I/O module

## Objective

Implement dkg.io with functions to load numeric matrices from disk and align two matrices on their shared observation index.

## Acceptance Criteria

- [ ] `load_matrix("file.csv")` returns a tuple of (float64 ndarray, row_labels list, col_labels list)
- [ ] .feather and .arrow inputs load correctly via polars
- [ ] .csv.gz loads correctly
- [ ] `align_matrices(X, X_rows, Y, Y_rows)` inner-joins on shared row labels and returns aligned arrays
- [ ] align_matrices raises ValueError with a clear message if fewer than 10 observations survive alignment
- [ ] All-NaN matrix raises ValueError
- [ ] All default checks pass

## Inputs

- src/dkg/io.py
- src/dkg/config.py

## Outputs

- src/dkg/io.py
- tests/test_io.py

## Requirements

- `load_matrix(path: str) -> tuple[np.ndarray, list[str], list[str]]` returns (data, row_labels, col_labels)
- Route to polars for .feather/.arrow, numpy/polars for .csv/.csv.gz
- All data cast to float64; missing values become NaN; no imputation
- `align_matrices(X: np.ndarray, X_rows: list[str], Y: np.ndarray, Y_rows: list[str]) -> tuple[np.ndarray, np.ndarray, list[str]]` inner-joins on shared row labels
- Raise ValueError if fewer than 10 shared rows survive alignment
- Raise ValueError if any matrix is all-NaN
- Raise ValueError if column count < 1
- Tests cover: CSV round-trip, feather round-trip, alignment with partial overlap, alignment failure below 10 rows, all-NaN rejection

## Blocked By

- 002

## Reviewer Notes

VERDICT: PASS
CRITERIA:
- **`load_matrix` returns `(float64 ndarray, row_labels, col_labels)`** — `io.py:25-28` casts to `pl.Float64` then `np.float64`, returns the three-tuple; `test_load_csv_roundtrip` asserts `dtype == np.float64` and shape.
- **`.feather` and `.arrow` load via polars** — `io.py:16` routes both suffixes to `pl.read_ipc`; `test_load_feather_roundtrip` and `test_load_arrow_roundtrip` verify round-trips.
- **`.csv.gz` loads correctly** — `io.py:18` includes `.csv.gz` in the branch; `test_load_csv_gz_roundtrip` writes a gzip-compressed CSV and asserts shape/dtype.
- **`align_matrices` inner-joins on shared row labels** — `io.py:48-57` computes shared labels via dict lookup; `test_align_partial_overlap` confirms 15-row overlap from two 30-element lists; `test_align_preserves_values` verifies row correspondence.
- **ValueError when fewer than 10 observations survive alignment** — `io.py:52-55` raises with message "need at least 10"; `test_align_below_10_rows_raises` triggers 2-row overlap and matches `"at least 10"`.
- **All-NaN matrix raises ValueError** — `io.py:30-31` checks `np.all(np.isnan(data))`; `test_load_all_nan_raises` matches `"entirely NaN"`.
- **All default checks pass** — audit shows ruff, mypy, and 15 pytest tests all exit 0.

SUMMARY:
All acceptance criteria are met with concrete implementation and corresponding tests. The routing logic, dtype enforcement, alignment inner-join, and all three ValueError conditions are implemented correctly and verified by the passing test suite.
