---
id: 006
status: TODO
reviewer: sonnet
milestone: v1
---
# Task 006: Phase 2 port — global linear association

## Objective

Port summarize_global_linear_association() from fitting_functions.R to dkg.phases.phase2.

## Acceptance Criteria

- [ ] Output contains symmetric_pair_metrics and directional_edge_metrics dicts with R-matching field names
- [ ] Pearson, Spearman, Kendall values match scipy equivalents within atol=1e-10 on a synthetic test pair
- [ ] Distance correlation value is computed and present
- [ ] Both x->y and y->x directional metrics are present
- [ ] All default checks pass

## Outputs

- src/dkg/phases/phase2.py
- tests/test_phase2.py

## Requirements

- Symmetric metrics: Pearson r + 95% CI (Fisher z-transform) + p, Spearman rho + p, Kendall tau + p, distance correlation
- Distance correlation via scipy or manual implementation of the energy-based dCor statistic (NOT concordance correlation — the R function uses energy::dcor)
- Directional metrics (both x->y and y->x): OLS slope/intercept/r²/p via scipy.stats.linregress; robust slope via statsmodels.robust.robust_linear_model.RLM (Huber M-estimator); slope_ratio = robust_slope / (ols_slope + sign(ols_slope)*1e-8)
- Handle complete-case filtering: drop rows where either x or y is NaN before all computations
- All field names must match R output column names exactly

## See Also

- fitting_functions.R

## Blocked By

- 002
