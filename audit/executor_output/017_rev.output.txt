VERDICT: PASS

CRITERIA:
- **`run(config)` produces all four Parquet files in config.output_dir**: `tier0_marginals.parquet` is written explicitly at line 58 (`marginals.write_parquet(...)`); `tier1_screen.parquet`, `tier2_deep.parquet`, and `tier3_stability.parquet` are written by `screen()`, `run_deep()`, and `run_stability()` respectively (confirmed by `test_all_parquet_files_written` passing in the 303-test audit run).
- **tier0_marginals.parquet contains Phase 1 profiles with source column**: `sweep_phase1()` is called for X (line 49) and Y (line 50); both DataFrames are tagged with `pl.lit("x")` / `pl.lit("y")` as `"source"` (lines 53–56) before concatenation. Tests `test_tier0_has_source_column`, `test_tier0_source_values`, and `test_tier0_row_count` all verify structure and row count (`len(x_cols) + len(y_cols)` = 8 + 6 = 14).
- **All output files are non-empty and polars-readable**: `test_parquet_files_readable` reads all four files and asserts `isinstance(df, pl.DataFrame)`; all 303 tests passed.
- **Clear error raised if x_matrix_path or y_matrix_path is None**: Lines 27–28 raise `ValueError("xy mode requires both x_matrix_path and y_matrix_path to be set")`; `test_raises_without_x_path` matches `"x_matrix_path"` and `test_raises_without_y_path` matches `"y_matrix_path"`, both pass.
- **All default checks pass**: Audit shows ruff check, ruff format, mypy, and pytest all exit 0; 303 tests passed.

SUMMARY:
All five acceptance criteria are fully satisfied. The implementation correctly orchestrates load, align, Phase 1 sweep, tier1–3 execution, and tier0 marginals assembly with proper source labeling. Error handling for missing matrix paths is explicit and test-verified. The overlap warning uses `warnings.warn` (not `raise`) as required. All static analysis and test suite checks are green.
